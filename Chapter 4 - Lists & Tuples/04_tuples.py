#Creating a tuple using()
t = (1,2,4,5)
#t1 = () #empty touple
#t1 = (1) #Wrong way to declare a touple with single element
t1 = (1,) #touple with single element
print(t1)
#Printing elements of touple
#print(t[0])

#Cannot update values in touple
#t[0] = 44 #throws error